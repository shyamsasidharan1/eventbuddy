apiVersion: v1
kind: Service
metadata:
  name: eventbuddy-api-service
  namespace: eventbuddy
  labels:
    app: eventbuddy
    component: api
spec:
  selector:
    app: eventbuddy
    component: api
  ports:
  - name: http
    port: 80
    targetPort: 3001
    protocol: TCP
  type: ClusterIP